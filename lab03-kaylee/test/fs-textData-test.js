'use strict';

const expect = require('chai').expect;
const Buffer = require('buffer').Buffer;
const fs = require('fs');

describe('fs-textData.js', function() {
  describe('#readFileOne', function() {
    it('should be read in before two.txt or three.txt', function() {
      
    })
  })
})
